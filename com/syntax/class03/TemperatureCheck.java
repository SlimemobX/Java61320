package com.syntax.class03;

public class TemperatureCheck {
	
	public static void main(String[] args) {
		// Create variable to store temperature 
		// Your temperature should check if temperature if below 32.
		// should print "Water will freeze with temperature ____", otherwise "Water will not freeze with temperature ____."
		int temp = -32;
		int temp2 = 0;
		
		if (temp < temp2) { 
			System.out.println("Water will freeze with teperature " + temp);
		
			
		}else { 
			System.out.println("Water will not freeze with temperature " + temp2);
		}
	}

}
